apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: nginx-deployment
  namespace: flux-system
spec:
  interval: 10m
  path: "."
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-repo
  validation: client
